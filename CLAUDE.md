# CLAUDE.md — Magic Recipe: Logical

このファイルは Claude Code がプロジェクトを操作する際の指針です。実装・修正・レビューを行う前に必ず読んでください。

---

## プロジェクト概要

「Magic Recipe: Logical」は、ユーザーが入力した食材をもとに SE/エンジニアリングのメタファーで構成された「論理的なレシピ」を生成する、クライアントサイド完結の Web アプリです。

- **外部 API なし**：すべてのデータは静的定数として `src/data/` 内に保持
- **サーバーなし**：React + Vite によるフロントエンド単体で完結
- **コストゼロ**：デプロイ後は通信コストが一切発生しない設計

---

## 技術スタック

| 技術 | バージョン | 備考 |
|------|-----------|------|
| React | 18 | 関数コンポーネント・Hooks のみ |
| TypeScript | 5.x（strict） | `any` 型禁止 |
| Vite | latest | ビルドツール・開発サーバー |
| CSS Modules | — | `*.module.css` でスコープ管理 |
| Vitest | — | 将来のユニットテスト対応（純粋関数のテストを想定） |

---

## アーキテクチャ原則

```
src/
├── components/    UIコンポーネント（表示責務のみ）
├── data/          ワード配列・定数（1,000語超）
├── logic/         レシピ生成アルゴリズム（純粋関数）
└── hooks/         カスタムフック（ロジックとUIの橋渡し）
```

- `logic/` の関数は副作用を持たない純粋関数として実装する
- `data/` のワード配列は `as const` で型推論を確定させる
- コンポーネントはデータ取得・変換ロジックを持たない（hooks 経由で受け取る）

---

## コーディング規約

- **コンポーネント**：関数コンポーネントのみ（クラスコンポーネント禁止）
- **型**：`any` 禁止。型推論を最大限活用し、推論が難しい箇所のみ明示的に型付け
- **コメント**：「なぜ」が非自明な場合のみ記述。コードが「何をするか」は命名で表現する
- **エラー**：`console.log` は開発時のみ許可。本番コードには混入させない
- **スタイル**：CSS Modules を使用。グローバル CSS はリセットと CSS カスタムプロパティ（変数）定義のみ

---

## 絶対禁止事項（DO NOT）

以下の実装は、いかなる理由があっても行わないこと。

| 禁止事項 | 理由 |
|----------|------|
| 外部 API への `fetch` / `axios` 通信 | コンセプト（Zero API Cost）の根幹を破壊する |
| ネガティブ・攻撃的・性的な表現を含むワードの追加 | Clean Experience の原則に違反する |
| 重い CSS アニメーション・`@keyframes` による派手な演出 | Minimalist UI の原則に違反する |
| `eval()` の使用 | 任意コード実行リスク（セキュリティ） |
| `dangerouslySetInnerHTML` の使用 | XSS リスク（セキュリティ） |
| `console.log` の本番コードへの混入 | 情報漏洩リスク・コード品質低下 |
| 個人情報・機密情報の `localStorage` 保存 | プライバシーリスク |

---

## セキュリティ要件

- **入力サニタイズ**：ユーザーが入力した文字列は、DOM に直接挿入せず React の JSX テキストノードとしてのみ扱う
- **依存パッケージ**：`npm install` は公式 npm レジストリのパッケージのみ使用。不審なパッケージを追加しない
- **CSP**：将来的に `Content-Security-Policy` ヘッダーの設定を推奨（`script-src 'self'`、`connect-src 'none'` を基本とする）
- **情報露出**：`vite.config.ts` 等の設定ファイルに API キーや秘密情報を含めない（そもそも存在しないことが理想）

---

## 品質基準

- TypeScript の `strict` モードでエラーゼロ
- `npm run build` がエラー・警告なしで完了すること
- コンポーネントの責務が単一であること（1コンポーネント＝1責務）
- `data/` のワード配列に不適切な表現が混入していないこと（追加時は必ずレビュー）

---

## ライセンス

改変・商用利用・再配布を禁じます。
